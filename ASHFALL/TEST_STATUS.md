# ASHFALL Test Status

The repository last documented these checks as passing after Job 004:

- `gradle test`
- Headless engine runs with the default seed and tick count.
- Headless engine runs with custom chunk size and 128 x 128 grid settings.
- Existing browser-game checks remain available through the repository's pnpm
  scripts.

This local handoff adds documentation only. It does not claim that tests were
rerun here.

For every future job, record exact test and benchmark commands and outcomes
before marking the job complete.